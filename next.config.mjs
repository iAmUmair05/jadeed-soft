/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [ "i.pravatar.cc", "source.unsplash.com", "unsplash.com", "mdbcdn.b-cdn.net"]
    }
};

export default nextConfig;
